parameters:

services:
    exporter.formatter.dispatcher.json:
        class: %behat.formatter.dispatcher.class%
        arguments: [Wgcrouch\BehatExporter\Formatter\JsonFormatter, "json", "Outputs features as json"]
        tags: 
            - {name: behat.formatter.dispatcher}